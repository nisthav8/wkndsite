indices:
  wkndsite:
    include:
      - /magazine/all-magazine/**
    target: /query-index.json
    properties:
       title:
        select: main > div > div > p
        value: |
          match(el, 'by (.*)')