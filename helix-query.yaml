indices:
  wkndsite:
    include:
      - /magazine/all-magazine/**
    target: /query-index.json
    properties:
       title:
        selectFirst: main > div > div > p
        value: |
          textContent(el)