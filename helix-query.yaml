indices:
  wkndsite:
    include:
      - /magazine/all-magazine/**
    target: /query-index.json
    properties:
       description:
        selectFirst: main
        value: |
          match(el, 'by (.*)')